# 黄金预测系统依赖

# 基础
numpy>=1.21.0
pandas>=1.3.0

# 数据获取
yfinance>=0.2.0

# 机器学习
scikit-learn>=1.0.0
xgboost>=2.0.0
lightgbm>=4.0.0   # P1: LightGBM 替代 XGBoost（同接口，更快更鲁棒）

# 时间序列
statsmodels>=0.13.0
pmdarima>=1.8.0  # ARIMA 自动参数优化

# 深度学习（可选，LSTM实验用）
# tensorflow>=2.10.0  # 注释掉，XGBoost 已替代 LSTM/GBM 需求
# tensorflow-metal  # Mac M1/M2 GPU 加速（可选）
# tensorflow-cpu  # 无GPU时使用CPU版本

# 可视化
plotly>=5.0.0
chart-studio>=1.1.0

# 数据库（云端部署）
psycopg2-binary>=2.9.0
sqlalchemy>=1.4.0

# Web 服务
flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=20.0.0

# 日志与工具
python-dotenv>=0.19.0
schedule>=1.1.0
requests>=2.26.0

# 异步任务（可选）
celery>=5.0.0
redis>=3.5.0
